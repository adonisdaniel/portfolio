// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://adonisdaniel.github.io/portfolio/',
  base: '/', // Solo si NO es tu-usuario.github.io
});
